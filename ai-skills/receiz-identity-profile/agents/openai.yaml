interface:
  display_name: "Receiz Identity Profile"
  short_description: "Update the admitted account profile"
  default_prompt: "Use $receiz-identity-profile to update the authenticated Receiz profile through the neutral same-UID operation."
