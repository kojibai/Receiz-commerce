interface:
  display_name: "Receiz Testing"
  short_description: "Test laws replay and compatibility"
  default_prompt: "Use $receiz-testing to prove exact artifact bytes, digests, Signature V4, owner/claim binding, cross-app continuity, and failure refusal."
