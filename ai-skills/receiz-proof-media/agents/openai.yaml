interface:
  display_name: "Receiz Proof Media"
  short_description: "Create native sealed proof media"
  default_prompt: "Use $receiz-proof-media to create and preserve native Record -> Seal media, then project only its verified URL to the same-UID profile."
