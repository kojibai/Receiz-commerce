interface:
  display_name: "Receiz Release"
  short_description: "Qualify a constitutional release"
  default_prompt: "Use $receiz-release to release-lock one registry and artifact-law version with complete native artifact evidence."
