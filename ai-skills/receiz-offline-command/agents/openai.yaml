interface:
  display_name: "Receiz Offline Command"
  short_description: "Queue transport intent beneath proof truth"
  default_prompt: "Use $receiz-offline-command to persist transport intent without calling queue settlement proof admission."
