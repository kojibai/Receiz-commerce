# Receiz v126 SDK upgrade

The application now pins `@receiz/sdk`, `@receiz/mcp-server`, and `@receiz/ai-skills` to `126.0.0`. The application package remains `5.2.1`.

- Ruleset: `126.0.0`
- Compatible SDK range: `>=126.0.0 <127.0.0`
- Canonical registry: `80137c2e6f294050ef36ff75e4daac15c7790b7f04d9a91fab9d1970fa3c0b09`
- Operation matrix: `42c7f0924df91b4ba11c1b891fee2b92abb509430a86b030735c23d055e67949`
- Application registry: `a7873cde93d169c21900d730bd2482a83455c79c4ed046a609b607138fe194c0`
- Inventory: 60 operations, 221 MCP tools, 43 skills, 37 manifests, 34 agent prompts.

The current app law overlay inherits the package's canonical registry and retains its six custom runtime authority laws. Its current-release rule accepts v126. The SDK-generated operation boundaries and capabilities are regenerated from the full current matrix. Existing v124 runtime adapters and routes remain supported. Historical migration evidence and proof records are unchanged; no production data migration or deployment is performed.

## Published-package discrepancies

The AI package's `counts` summary still reports 42/36/33; enumerating its actual skill entries yields 43/37/34. The local copy retains exact published bytes, and the app displays the enumerated totals.

The upstream integration scanner applies application-code regexes to the package's function inventory. It reports deprecated-API and weaker-state-authority findings in three published inventory files. `pnpm receiz:check` reports the raw upstream status and these reviewed findings separately. Exceptions require the exact finding/path combinations and byte equality with the installed package. Any changed file, additional application path, or unknown finding fails the check. Regression tests exercise those rejection cases.

The upstream streaming-verifier export gap observed in v124.0.3 remains present in v126.0.0 and remains tracked privately in `receiz.upstream-gaps.json`. The application does not deep-import the unavailable verifier subpath.

## Validation

Use `pnpm test`, `pnpm typecheck`, `pnpm lint`, `pnpm validate:ai-skills`, `pnpm receiz:cli:check`, `pnpm receiz:release-lock`, and `pnpm build`. The v126 release lock checks exact dependencies and integrity, registry digests, generated matrices, AI byte parity, retained runtime custody and verification boundaries, focused regressions, and SDK conformance.

Verified locally: all 809 tests pass; typecheck, AI-skill validation, CLI checks, migration verification, release lock, doctor, secret scan, and production build pass. Lint has no errors and retains the existing image-element warning. The build also reports upstream `web-worker` dynamic-dependency warnings through the SDK's `snarkjs` dependency.
