# Receiz v124.0.1 Production Runtime Release Plan

1. Pin SDK, MCP, and AI-skills packages at exact `124.0.1` coordinates and record
   their registry integrities.
2. Regenerate the app contract from the SDK's 53-operation matrix and chain the
   app constitution to the canonical v124 registry under ruleset `124.0.0`.
3. Mirror and validate the published 42-skill AI doctrine without expanding any
   manifest's MCP grants.
4. Extend the SDK adapter with every v124 client rail and canonical local helper.
5. Implement branded trusted-host custody for authority sessions, durable plans,
   handles, private additions, replay candidates, and sealed-source references.
6. Implement qualification-gated atomic stage/execute/cancel and mandatory
   lookup-before-retry recovery for unknown outcomes.
7. Implement verified replay/checkpoint/private reads, exact-head namespace
   resolution, privacy-safe recipient resolution, locator-bound Phi planning,
   and sealed-source publication.
8. Publish a non-authoritative runtime readiness API and the complete public
   developer doctrine/tool map.
9. Add v124 negative tests, authority scanning, migration verification, and a
   release lock tied to exact package, registry, matrix, skill, and custody facts.
10. Bump the application to 5.2.0, update release documents, run all release
    checks, and create the local release commit and tag without pushing.
